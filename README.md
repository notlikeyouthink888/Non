# Your World

تطبيق أندرويد شخصي بأحد عشر قسمًا — **كل شيء يعمل دون إنترنت**، وكل البيانات تبقى داخل جهازك:
لا حساب، ولا خادم، ولا مزامنة.

| القسم | ما فيه |
|---|---|
| ♪ **الأغاني** | كل أغاني جهازك، تشغيل بالخلفية وعلى شاشة القفل، تكرار وعشوائي، سرعة التشغيل، مؤثرات صوتية، تحديث يكتشف ما نزّلته من تيليجرام، وتغيير اسم أي أغنية |
| ⏰ **الوقت** | مؤقّت وساعة إيقاف، منبّهات بنغمة مخصّصة وتكرار متقدّم، تقويم ومهام، حاسبة نوم، ملخّص نهاية اليوم، وتحذير الاستخدام المطوّل |
| 🧭 **نظام S2** | جدول وقتك بالدقيقة: أيام دوام وعطلة، كتلة الدوام الثابتة، وكتل للقراءة والنوم والهواية والجم، مع «الآن والتالي» وإحصاء أسبوعي وربط اختياري بالتمارين |
| 📚 **المذاكرة** | مجموعات ← صفحات بلا حدّ ← عناصر: نص وعناوين واقتباسات وصور وملاحظات رسم، وملفات **PDF تُقرأ ويُكتب عليها داخل التطبيق نفسه** |
| 🚀 **Improve** | تطوير الذات: مجالات ← خطوات، كل خطوة بنصّها وصورها وفيديوهاتها، أسطر «شنو أحتاج» (مبلغ/غرض/وقت)، إنجاز **بتأكيد ودليل**، وربط بالتقويم يضيف مهمّة جديدة دون المساس بمهامك القديمة |
| 🏋 **التمارين** | ثلاثة أيام ثابتة × ٨ خانات تمارين، كل خانة تقبل نصًا أو صورة أو فيديو، مع حساب البروتين اليومي وخطّة لكل يوم — قائمة أطعمة فيها الفواكه والخضروات بمقدار لكل حبة/١٠٠ غم/نصف كيلو/كيلو، وكل رقم قابل للتعديل |
| 💰 **المصاريف** | كل ما تشتريه بسعره، كم يكفيك وتكلفته اليومية، الاشتراكات المتكرّرة، وتقرير شهري |
| 🎯 **التزاماتي** | ٥٧ تمرينًا وطريقة استرخاء بخطواتها وأفضل وقت لها، ٦ روتينات جاهزة، وتتبّع يومي بالسلاسل |
| 🗺 **الأماكن** | خريطة تعمل بلا إنترنت، أماكن تريد زيارتها بتفاصيلها، مسافة واتجاه من موقعك |
| 🛏 **غرفتي** | أشياء لازم أضيفها لغرفتي بترتيبها وأولويتها وأماكنها وصورها، وربطها بالمصاريف عند الشراء |
| 📈 **الإنتاجية** | ١٢٤ عنصرًا عمليًا في ١٢ قسمًا، ٦ مسارات مرتّبة، وتتبّع ما أتقنته |

في كل قسم تقريبًا يمكنك إضافة **ملاحظة رسم** بقلم سامسونغ (S Pen) — بضغط حسّاس
ورفض لراحة اليد وصفحات تمتدّ بلا نهاية.

في **عارض PDF** داخل التطبيق: تمرير منتظم لا تقفز فيه الصفحات، تكبير بإصبعين
يحافظ على موضع قراءتك، شريط مصغّرات وبحث في النصّ — و**رسم وكتابة فوق الصفحات**
بقلم ومُبرِز وخط وإطار ونصّ وممحاة، ٢٤ لونًا وخمس سماكات، مع تراجع وإعادة.
ما ترسمه يُحفظ لكل صفحة ويبقى مع الملف.

النصّ العربي يُرسم من خطّ الملف نفسه لا من خطوط النظام، فلا تتبعثر الحروف ولا
يسقط منها شيء. وأثناء الرسم يبقى **التكبير مقفلًا** (🔒 قابل للفتح) وتُتجاهَل
راحة يدك، فلا تكبر الصفحة وتصغر تحت قلمك.

الشريط السفلي يعرض أربعة أقسام تختارها أنت، وزرّ **الأقسام ⋯** يفتح الأحد عشر كلها
(اضغط 📍 على أي قسم لتثبيته في الشريط).

---

## التشغيل

```bash
npm install          # مرّة واحدة
npm run dev          # معاينة في المتصفّح (http://localhost:5173)
npm run check        # فحص آلي: بناء + تصفّح كل الأقسام + اختبار المسارات
npm run android:sync # بناء + مزامنة مشروع أندرويد
npm run android:apk  # بناء ملف APK (يحتاج Android SDK)
```

في المتصفّح تعمل كل الأقسام، لكن مكتبة الأغاني تصبح باختيار ملفات يدويًا،
والمنبّهات تعمل ما دامت الصفحة مفتوحة. على أندرويد تعمل جميعها أصليًا.

### بناء ملف APK

يحتاج جهازك: Node 20+، JDK 17+، وAndroid SDK (platform 35 + build-tools).

```bash
npm run android:apk
# الناتج: android/app/build/outputs/apk/debug/app-debug.apk
```

> ملاحظة: البناء يتطلّب تنزيل Android SDK وحزم Gradle من خوادم Google،
> لذا نفّذه على جهازك أو في CI يملك هذا الوصول.

---

## ما الذي يحتاج إنترنت؟ لا شيء تقريبًا

- كل الأقسام تعمل بلا اتصال: المشغّل، المنبّهات، التقويم، التمارين وصورها وفيديوهاتها،
  المصاريف، الالتزامات، الغرفة، الأماكن، والمسافات — وشبكة إحداثيات الخريطة تُحسب داخل الجهاز.
- الاستثناء الوحيد وهو **اختياري بالكامل**: زرّ «نزّل خريطة هذه المنطقة» في
  قسم الأماكن يجلب صور الخريطة مرّة واحدة ويخزّنها في جهازك، فتعمل بعدها دون اتصال.
  إن لم تضغطه، لا يخرج أي طلب شبكة من التطبيق أبدًا.
- إذن `INTERNET` في البيان مطلوب لخادم Capacitor المحلي داخل الجهاز نفسه.

---

## الأذونات ولماذا

| الإذن | السبب |
|---|---|
| `READ_MEDIA_AUDIO` (أو `READ_EXTERNAL_STORAGE` قبل أندرويد 13) | قراءة أغاني جهازك |
| `FOREGROUND_SERVICE` + `..._MEDIA_PLAYBACK` | استمرار التشغيل والشاشة مقفلة |
| `POST_NOTIFICATIONS` | إشعار المشغّل، المنبّهات، التذكيرات |
| `SCHEDULE_EXACT_ALARM` / `USE_EXACT_ALARM` | رنين المنبّه في وقته بالضبط |
| `USE_FULL_SCREEN_INTENT` | شاشة المنبّه فوق شاشة القفل |
| `RECEIVE_BOOT_COMPLETED` | إعادة جدولة منبّهاتك بعد إعادة تشغيل الجهاز |
| `ACCESS_FINE_LOCATION` | موقعك في قسم الأماكن (GPS يعمل دون إنترنت) |
| `WRITE_EXTERNAL_STORAGE` (حتى أندرويد ٩ فقط) | حفظ نسختك الاحتياطية في مجلّد التنزيلات — من أندرويد ١٠ فصاعدًا لا يُطلب أي إذن |
| الكاميرا/المعرض | تُفتح عبر منتقي النظام لإضافة صور وفيديو التمارين وأغراض الغرفة — بلا إذن دائم |
| `VIBRATE` / `WAKE_LOCK` / `MODIFY_AUDIO_SETTINGS` | الاهتزاز، إيقاظ الشاشة للمنبّه، ومؤثرات الصوت |

كل الأذونات تُطلب عند الحاجة فقط، والتطبيق يعمل بدون أغلبها مع فقدان الميزة المرتبطة بها.

---

## بنية المشروع

```
src/
  main.js            نقطة الدخول
  app.js             الشِل: التنقّل بين الأقسام + المشغّل المصغّر
  core/              store, dom, ui, fmt, idb, media, draw, pdf, usage, settings, native
  sections/
    music/           المكتبة، المشغّل، المؤثرات، شاشة التشغيل، إعادة التسمية
    time/            المؤقّت، المنبّهات، التكرار، التقويم، النوم، ملخّص اليوم
    s2/              نظام S2 لتنظيم الوقت
    study/           المذاكرة: مجموعات وصفحات وعناصر
    workout/         ٣ أيام × ٨ خانات تمارين + البروتين
    improve/         تطوير الذات: مجالات وخطوات بأدلّة إنجاز وربط بالتقويم
    money/           المصاريف والاشتراكات والتقارير
    commit/          الالتزامات والتمارين الجاهزة
    places/          الخريطة والبلاطات والحسابات الجغرافية
    room/            أشياء الغرفة وترتيبها
    growth/          الإنتاجية
  data/              exercises.js (٥٧) · growth.js (١٢٤) · protein.js
  styles/            CSS لكل قسم

android/app/src/main/java/com/yourworld/app/
  MainActivity.java
  plugins/           MusicLibrary, Player, Scheduler, Files
  player/            PlayerService (خدمة أمامية + MediaSession + مؤثرات)
  alarm/             AlarmScheduler, AlarmReceiver, AlarmActivity, RepeatRule, BootReceiver
```

التفاصيل المعمارية الكاملة في [`ARCHITECTURE.md`](./ARCHITECTURE.md).

---

## بياناتك

كل شيء داخل جهازك. المرجع هو **IndexedDB** (سعة كبيرة، لا يتأثّر بحدّ الخمسة
ميغابايت)، ومعه مرآة في `localStorage` لإقلاع فوري. الكتابة مؤجَّلة ٢٥٠ ملّي ثانية
لتجميع التعديلات، وتُنفَّذ فورًا عند الخروج أو إخفاء التطبيق. إن امتلأت المساحة
يُقلَّم أقدم سجلّ استخدام ثم يُعاد الحفظ، وإن فشل يظهر تنبيه — لا فقدان صامت.

**النسخ الاحتياطي** (الإعدادات ⚙ ← 🗄 النسخ الاحتياطي والاسترجاع):

- **احفظ في مجلّد التنزيلات** — يُكتب الملف فعليًا في `Download/YourWorld/`
  ويُعرض لك مساره، مع نسخة داخلية إضافية.
- **ابحث عن نسخي في الجهاز** — يمسح فهرس النظام ومجلّدات التنزيل، ويعرض لك ما في
  كل نسخة (كم تمرينًا، كم مهمّة…) قبل استرجاعها، مع خيار **الدمج** بدل الاستبدال.
- **نسخ تلقائية داخل التطبيق** — لقطة كل ست ساعات، وقبل كل استيراد أو مسح،
  يُحتفظ بآخر اثنتي عشرة لقطة، وكلّها قابلة للاسترجاع بضغطة.

> ملاحظة: النسخة الاحتياطية تشمل النصوص والأرقام، أما ملفات الصور والفيديو والرسوم
> فتبقى في تخزين الجهاز ولا تدخل في ملف JSON.

المشروع السابق (لعبة «مدينتي» ثلاثية الأبعاد) محفوظ في مجلّد `legacy/`.
